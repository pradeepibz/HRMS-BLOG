%tr
  %td= language.name
  %td= link_to "Edit", edit_language_path(language), remote: true
  %td= link_to "Delete", language_path(language), "data-confirm" => "Are you sure?", remote: true, method: :delete
